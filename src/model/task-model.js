class taskModel{
    constructor(id,title,description,status,data){
         this.taskId= id,
         this.title= title,
         this.decription= description,
         this.status= status,
         this.data= data
   }
}
module.exports = taskModel;